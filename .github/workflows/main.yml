name: Hello World
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Print Hello World
      run: |
        response=$(curl -s https://example.com)  # Replace https://example.com with your desired URL
        echo "$response" >> $GITHUB_STEP_SUMMARY
