name: Hello World
on:
  workflow_dispatch:
    inputs:
      input_1:
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      test: "test1,test2,"

    steps:
    - name: Print Hello World
      run: |
        response=$(curl -s https://reqres.in/api/users/2 | jq -r '.data | "- id: \(.id), email: \(.email)"')
        echo '### The response is 🚀' >> $GITHUB_STEP_SUMMARY
        echo "$response" >> $GITHUB_STEP_SUMMARY
        echo $test
        echo "${{env.test}}${{inputs.input_1}}" > test
        echo "$test
