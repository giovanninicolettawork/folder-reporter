name: Hello World
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Print Hello World
      run: |
        response=$(curl -s https://reqres.in/api/users/2)
        echo '### The response is 🚀' + "$response" >> $GITHUB_STEP_SUMMARY
